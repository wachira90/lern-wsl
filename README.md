# lern-wsl
lerning windows subsystem linux

```sh
cat /mnt/c/Users/Wachira/.wslconfig

[wsl2]
memory=16GB
processors=4
swap=0



wsl --shutdown


nano /etc/resolv.conf

nameserver 8.8.8.8

chattr +i /etc/resolv.conf


cat /etc/resolv.conf
```
